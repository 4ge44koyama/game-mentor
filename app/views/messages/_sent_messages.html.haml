%li.sent-lists__list
  .sent-lists__list__wrapper
    .sent-lists-from
      = sent_message.to_user.name
      %span さんへ
    .sent-lists-content
      = sent_message.content
    .sent-lists-time
      = sent_message.created_at.strftime("%Y年%m月%d日 %H時%M分")